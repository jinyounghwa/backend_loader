boto3>=1.26.0,<1.35.0
botocore>=1.29.0,<1.35.0
# requests 2.32.4+ fixes CVE-2024-35195 (cert-verify bypass on session reuse)
# and CVE-2024-47081 (.netrc credential leak to arbitrary hosts)
requests>=2.32.4
python-telegram-bot==20.3
discord.py==2.3.2
python-dotenv==1.0.0
PyNaCl==1.5.0
apscheduler==3.10.4
google-generativeai==0.3.0
fpdf2==2.7.0
pydantic>=2.0.0
redis>=4.5.0
aioboto3==12.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
# Optional ML forecasting (ARIMA). Imports are guarded with try/except so the
# package degrades gracefully if these are absent, but they are required for
# the cost/threat ARIMA forecasting features to function.
statsmodels>=0.14.0
pmdarima>=2.0.4
httpx==0.24.1
pytest-asyncio>=0.21.0
